<form class="subscribe-form" action="{{ site.subscribeFormAction }}" method="POST">
  <input type="hidden" name="_subject" value="Future Shock Media — Publication subscribe">
  <input type="hidden" name="_next" value="{{ site.url }}{{ page.url }}">
  <label class="sr-only" for="subscribe-email">Email</label>
  <input class="subscribe-input" type="email" id="subscribe-email" name="email" placeholder="Your email" required autocomplete="email">
  <button type="submit" class="subscribe-submit">Subscribe &rarr;</button>
</form>
